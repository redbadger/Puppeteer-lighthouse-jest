{
  "name": "puppeteer-lighthouse-jest",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "jest --verbose"
  },
  "devDependencies": {
    "jest": "^23.4.1",
    "lighthouse": "^3.0.3",
    "puppeteer": "^1.6.0"
  }
}
